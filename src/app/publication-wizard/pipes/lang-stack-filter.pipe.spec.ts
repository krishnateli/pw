import { LangStackFilterPipe } from './lang-stack-filter.pipe';

describe('LangStackFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LangStackFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
