import { PreviewFilterPipe } from './preview-filter.pipe';

describe('PreviewFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
